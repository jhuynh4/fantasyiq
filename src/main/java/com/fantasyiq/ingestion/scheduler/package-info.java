/**
 * Scheduled job orchestration (@Scheduled methods) and ingestion_runs bookkeeping. Depends on the provider interfaces above, never on their vendor-specific implementations directly.
 */
package com.fantasyiq.ingestion.scheduler;
