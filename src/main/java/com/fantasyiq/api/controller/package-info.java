/**
 * REST controllers. Thin -- validate input, delegate to analytics/domain services, map to DTOs. This is the ONLY package allowed to hold @RestController classes (enforced by ArchitectureRulesTest).
 */
package com.fantasyiq.api.controller;
